name: server
description: backend server for wandering compass
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.9.0
resolution: workspace

dependencies:
  dart_frog: ^1.1.0
  dart_frog_auth: ^1.2.2
  postgres: ^3.5.9
  shared:
    path: '../shared'
  stormberry: ^0.18.0

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.3
  test: ^1.25.5
