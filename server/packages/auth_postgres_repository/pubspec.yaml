name: auth_postgres_repository
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies: 
  auth_repository:
    path: '../auth_repository'
  compass_datasource:
    path: '../compass_datasource'
  dart_jsonwebtoken: ^3.3.1
  dbcrypt: ^2.0.0
  shared:
    path: '../../../shared'
  uuid: ^4.5.2

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
